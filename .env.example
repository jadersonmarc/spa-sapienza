# ── Banco (Postgres) ─────────────────────────────────────────────────────────
# Connection string do Postgres (Drizzle usa para migrations e queries).
# Local: host público da VPS. Produção (app na VPS): host interno do Docker. Ex.:
# postgres://postgres:[PWD]@[HOST]:5432/postgres
DATABASE_URL=

# ── Auth.js (login do /admin: Credentials, sessão JWT) ───────────────────────
# Segredo para assinar a sessão. Gerar com: openssl rand -base64 32
AUTH_SECRET=

# ── Storage de imagens (S3/MinIO self-hosted no Coolify) ─────────────────────
S3_ENDPOINT=
S3_REGION=
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
# Base pública para servir as imagens enviadas. Ex.: https://cdn.exemplo.com/bucket
S3_PUBLIC_URL=

# ── Claude API (geração + recursos de IA) ────────────────────────────────────
ANTHROPIC_API_KEY=

# ── Webhooks (auth máquina-a-máquina) ────────────────────────────────────────
# Validado em /api/generate-draft (entrada) e enviado ao n8n no publish (saída).
WEBHOOK_SECRET=
# URL do webhook do n8n (saída na publicação).
N8N_PUBLISH_WEBHOOK_URL=

# ── Publicação social (n8n usa; aqui p/ referência) ──────────────────────────
# Page token do Facebook (EAA) de longa duração — publica no Instagram.
INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_ACCOUNT_ID=
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_AUTHOR_URN=
